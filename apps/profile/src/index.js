// profileApp container entry. A federated remote registers no root component the way a normal app
// does; it waits to be loaded by a host. This is the container's entry point, and for now it has
// nothing to do at startup, so it stays empty.
export {};
